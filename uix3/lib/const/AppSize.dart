import 'package:flutter/material.dart';

class AppSize {
  static const double preferredSize = 250;

  static const appBarSize = EdgeInsets.symmetric(vertical: 25, horizontal: 10);
  static const appbarRadiusLeft = Radius.circular(30);
  static const appbarRadiusRight = Radius.circular(30);
  static const double appBarTextFontSize = 25;
  static const double iconScaleDesign = 2.5;
  static const double iconScaleCoding = 2;
  static const double iconScaleMarketing = 2;
  static const double iconScaleSecurity = 0.9;

  static const columPadding =
      EdgeInsets.symmetric(horizontal: 10, vertical: 20);
  static const boxDecoration = BorderRadius.all(Radius.circular(10));
  static const double uxUiText = 20;
  static const double learnDesignText = 12;
}
